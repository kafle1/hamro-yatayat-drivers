import 'package:flutter/material.dart';

const kThemeColor = Color(0xff0C3CFF);

const kBoxShadow = BoxShadow(
    color: Color.fromRGBO(0, 0, 0, 0.25), offset: Offset(0, 2), blurRadius: 10);

const kTitleTextStyle = TextStyle(fontWeight: FontWeight.bold, fontSize: 20);

const kDetailsLabelStyle = TextStyle(fontWeight: FontWeight.bold);
